import * as fs from "fs";
import * as readline from "readline-sync";
import { Users } from "./types/users";

const name: string = readline.questionEMail("Ingrese su correo => ");

const usuario: Users;
